#!/usr/bin/env ruby
$LOAD_PATH.push File.join(File.dirname(__FILE__),".." ,"lib/lineman" )
require 'lineman'
require 'utilities/comm_helper'

sender = STDOUT
lineman = Lineman::Core.new(sender)
lineman.start
comm = Lineman::CommHelper.new(sender)
while input = gets
  comm.receive(input)
end
